  

  <nav class="main-nav" id="pres-listing">
    <div class="close">
      <a href="#" id="close-listing"> ☰ </a>
    </div>
    <% @presentations.each do |p| %> 

    <div class="row-name">
     <%= link_to  p.name, edit_presentation_path(p), {target: "preview", :class=> "name" } %>
     <%= link_to raw('<i class="icon-laptop "></i>'), presentation_path(p), {target: "_blank", :class => '' }  %>
     <%= link_to raw('<i class="icon-remove"></i>'), p, confirm: 'Are you sure?', method: :delete, :class => 'remove' %>

    </div>

    <% end %>
      <div class="new">
        <%= link_to "New", method: :new %>
     </div>


</nav>